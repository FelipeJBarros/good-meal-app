import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container: {
        height: 58,
        maxHeight: 58
    },
    ingredientContent: {
        gap: 12,
        paddingHorizontal: 24
    }
});