import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container: {
        flexWrap: "wrap",
        flexDirection: "row",
        gap: 12,
        
        paddingTop: 4,
        marginBottom: 200
    }
})